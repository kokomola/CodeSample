import { log } from '@utils/debug';
import { $accountBalances, $balances, $tokenBalances } from './index';

// debug

//$balances.watch((balances) => log('$store/balance', { balances }));

/* $accountBalances.watch((accountBalances) =>
  log('$store/balance', { accountBalances })
); */
/* $tokenBalances.watch((tokenBalances) =>
  log('$store/balance', { tokenBalances })
); */
