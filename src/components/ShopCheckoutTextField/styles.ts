import { StyleSheet } from 'react-native';
import { independence400 } from '@constants/colors';

export const styles = StyleSheet.create({
  container: {
    height: 35,
    justifyContent: 'space-between',
    marginTop: 30,
    marginHorizontal: 16,
  },
  separator: {
    height: 1,
    width: '100%',
    backgroundColor: independence400,
  },
});
