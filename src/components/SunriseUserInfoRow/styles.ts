import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    marginTop: '2%',
    marginBottom: '3%',
    color: 'black',
  },
});
