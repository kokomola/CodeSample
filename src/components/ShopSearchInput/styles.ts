import { space200 } from '@constants/colors';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputBox: {
    marginTop: 10,
    marginBottom: 5,

    marginHorizontal: 14,
    paddingHorizontal: 10,

    backgroundColor: space200,
    borderRadius: 8,
    borderBottomWidth: 0,
  },
});
