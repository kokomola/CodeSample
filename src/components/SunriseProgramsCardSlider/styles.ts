import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  carousel: {
    marginLeft: 6, // no other way to align 'stack' carousel;
  },
  hint: {
    marginVertical: '5%',

    fontSize: 13,
    color: '#7a7f8a',
  },
});
