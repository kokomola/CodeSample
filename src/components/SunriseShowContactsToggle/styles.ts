import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  switchWrapper: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 9,
    marginVertical: 20,
  },
  text: { color: 'black', fontSize: 16, marginLeft: 10 },
});
