import { space300 } from '@constants/colors';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  box: {
    borderWidth: 1,
    borderColor: space300,
  },
  svg: {
    width: '100%',
    height: 'auto',
  },
});
